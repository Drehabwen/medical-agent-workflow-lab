[project]
name = "medical-agent-workflow-lab"
version = "0.1.0"
description = "Technical experiments for auditable medical AI agent workflows."
requires-python = ">=3.10"
authors = [{name = "Drehabwen"}]
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["src"]
